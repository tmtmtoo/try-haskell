module Lesson
  ( reverseList
  , sortList
  , isPalindrome
  , myGcd
  ) where

reverseList :: [a] -> [a]
reverseList = undefined

sortList :: Ord a => [a] -> [a]
sortList = undefined

isPalindrome :: Eq a => [a] -> Bool
isPalindrome = undefined

myGcd :: Integer -> Integer -> Integer
myGcd = undefined
