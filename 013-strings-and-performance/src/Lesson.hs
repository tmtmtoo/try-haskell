module Lesson
  ( wordCount
  , strictSum
  , runningMean
  , countLines
  ) where

import Data.ByteString (ByteString)
import Data.Map.Strict (Map)
import Data.Text (Text)

wordCount :: Text -> Map Text Int
wordCount = undefined

strictSum :: [Int] -> Int
strictSum = undefined

runningMean :: [Double] -> [Double]
runningMean = undefined

countLines :: ByteString -> Int
countLines = undefined
